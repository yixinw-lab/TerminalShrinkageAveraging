# app_b_7_groupwise_terminal_floors

Paper-facing exports are rebuilt by `python experiments/app_b_7_groupwise_terminal_floors/analyze.py` from the repository root. Use the [experiment entrypoint](../../experiments/app_b_7_groupwise_terminal_floors/) for the complete mapping.

The underlying compact run records, manifests and frozen/protocol files are included in `results/_sources/`. Exports are derived; original observations are not overwritten. The original interval and contrast conventions are preserved. Run `python experiments/_tools/verify_groupwise_records.py` from the repository root for CPU-only consistency checks; its report is written under the Git-ignored `.checks-local/` directory.
