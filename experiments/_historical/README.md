# Historical plotting code

The preliminary replot patch is retained as source history. It uses an earlier
manuscript numbering scheme. It is not the plotting entrypoint for the current
13-figure manuscript; use the section-indexed analysis scripts instead.
